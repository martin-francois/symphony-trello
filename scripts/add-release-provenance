#!/bin/bash

set -euo pipefail

if [[ ! -f "$PROVENANCE_BUNDLE" ]]; then
  echo "release provenance bundle was not created" >&2
  exit 2
fi
cp "$PROVENANCE_BUNDLE" "$ASSET_DIR/symphony-trello-$RELEASE_VERSION.intoto.jsonl"
